# Run this once if these packages are missing.
# Installation needs internet access. The analysis itself uses local files.
install.packages(c("readr", "readxl", "lmtest", "sandwich", "car",
                   "nortest", "stargazer"))
